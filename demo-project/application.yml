server:
  port: 8080

spring:
  application:
    name: demo-service
  h2:
    console:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
      cors:
        allowed-origin-patterns: "*"
        allow-credentials: true
  endpoint:
    shutdown:
      enabled: false