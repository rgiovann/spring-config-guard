spring.application.name=spring-env-benchmark
# 10. mesma chave que "[security.protocol]" em application.yml, escrita com pontos: .properties vence
app.bracket-map.security.protocol=SASL_SSL
# 13. chave entre colchetes em .properties, sobrescrita por application-prod.yml
app.bracket-map[props.bracketed]=from-properties
